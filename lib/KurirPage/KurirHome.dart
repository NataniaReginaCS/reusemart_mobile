import 'package:flutter/material.dart';

class KurirHome extends StatelessWidget {
  const KurirHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Hello Kurir',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}